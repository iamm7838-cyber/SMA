<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SDI — Chiến Lược Đầu Tư Thông Minh | VPS</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@300;400;500;600;700;800&family=Playfair+Display:ital,wght@0,700;0,900;1,700&display=swap" rel="stylesheet">
<style>
/* ─── TOKENS ─────────────────────────────────────── */
:root {
  --p900: #2E0070;
  --p700: #5B21B6;
  --p600: #6D28D9;
  --p500: #7C3AED;
  --p300: #A78BFA;
  --p100: #EDE9FE;
  --p50:  #F5F3FF;
  --ink:  #0D0D0D;
  --ink2: #3A3A3A;
  --ink3: #6B6B6B;
  --line: #E4E4E7;
  --bg:   #FFFFFF;
  --bg2:  #FAFAFA;
  --font-display: 'Playfair Display', Georgia, serif;
  --font-body: 'Be Vietnam Pro', sans-serif;
  --r-sm: 12px;
  --r-md: 16px;
  --r-lg: 24px;
  --max: 480px;
}

/* ─── RESET ──────────────────────────────────────── */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; -webkit-text-size-adjust: 100%; }
body {
  font-family: var(--font-body);
  background: var(--bg);
  color: var(--ink);
  overflow-x: hidden;
  line-height: 1.6;
}
img { max-width: 100%; display: block; }
a { text-decoration: none; }

/* ─── UTILITIES ──────────────────────────────────── */
.container {
  width: 100%;
  max-width: var(--max);
  margin: 0 auto;
  padding: 0 20px;
}
.tag {
  display: inline-block;
  font-size: 10px; font-weight: 700;
  letter-spacing: 1.8px; text-transform: uppercase;
  color: var(--p500);
  margin-bottom: 10px;
}
h2.ttl {
  font-family: var(--font-display);
  font-size: clamp(22px, 5.5vw, 32px);
  font-weight: 900; line-height: 1.2;
  color: var(--ink);
  margin-bottom: 12px;
}
h2.ttl em { font-style: normal; color: var(--p600); }
.desc {
  font-size: 14px; color: var(--ink3);
  line-height: 1.75; margin-bottom: 28px;
  font-weight: 400;
}
.btn-primary {
  display: inline-flex; align-items: center; gap: 6px;
  background: var(--p700);
  color: #fff; font-weight: 700; font-size: 14px;
  padding: 13px 28px; border-radius: var(--r-sm);
  border: none; cursor: pointer;
  transition: background 0.18s, transform 0.18s;
  font-family: var(--font-body);
}
.btn-primary:hover { background: var(--p600); transform: translateY(-1px); }
.btn-ghost {
  display: inline-flex; align-items: center; gap: 6px;
  background: transparent;
  color: var(--p600); font-weight: 700; font-size: 12px;
  border: 1.5px solid var(--p300);
  padding: 9px 18px; border-radius: var(--r-sm);
  transition: background 0.18s;
  font-family: var(--font-body);
}
.btn-ghost:hover { background: var(--p50); }

/* ─── NAV ─────────────────────────────────────────── */
nav {
  position: sticky; top: 0; z-index: 999;
  background: rgba(255,255,255,0.96);
  backdrop-filter: blur(16px) saturate(1.5);
  -webkit-backdrop-filter: blur(16px);
  border-bottom: 1px solid var(--line);
}
.nav-inner {
  display: flex; align-items: center; justify-content: space-between;
  height: 56px; padding: 0 20px;
  max-width: var(--max); margin: 0 auto;
}
.nav-logo {
  font-family: var(--font-body);
  font-weight: 800; font-size: 18px;
  color: var(--ink); letter-spacing: -0.5px;
}
.nav-logo span { color: var(--p600); }
.nav-btn {
  background: var(--p700);
  color: #fff; font-weight: 700; font-size: 12px;
  padding: 8px 18px; border-radius: 8px;
  transition: background 0.18s;
}
.nav-btn:hover { background: var(--p600); }

/* ─── HERO ────────────────────────────────────────── */
.hero {
  background: var(--p900);
  padding: 60px 20px 68px;
  text-align: center;
  position: relative;
  overflow: hidden;
}
.hero::after {
  content: '';
  position: absolute; inset: 0; pointer-events: none;
  background:
    radial-gradient(ellipse 80% 50% at 50% -10%, rgba(124,58,237,0.35) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 80% 110%, rgba(91,33,182,0.25) 0%, transparent 60%);
}
.hero-inner { position: relative; z-index: 1; max-width: var(--max); margin: 0 auto; }
.hero-badge {
  display: inline-block;
  border: 1px solid rgba(167,139,250,0.5);
  color: var(--p300);
  font-size: 10px; font-weight: 700;
  letter-spacing: 2px; text-transform: uppercase;
  padding: 5px 14px; border-radius: 100px;
  margin-bottom: 22px;
  background: rgba(167,139,250,0.08);
}
.hero h1 {
  font-family: var(--font-display);
  font-size: clamp(28px, 8vw, 46px);
  font-weight: 900; line-height: 1.12;
  color: #fff; margin-bottom: 16px;
}
.hero h1 em {
  font-style: italic;
  color: var(--p300);
}
.hero-sub {
  color: rgba(255,255,255,0.68);
  font-size: 14px; line-height: 1.75;
  margin-bottom: 6px;
}
.hero-sub2 {
  color: rgba(255,255,255,0.42);
  font-size: 12px; font-weight: 400;
  margin-bottom: 32px;
}
.hero-actions {
  display: flex; flex-direction: column; align-items: center; gap: 12px;
}
.hero-actions .btn-primary { font-size: 15px; padding: 15px 36px; border-radius: var(--r-md); }
.hero-actions .btn-outline {
  color: rgba(255,255,255,0.6); font-size: 13px; font-weight: 500;
  border: none; background: none; cursor: pointer;
  font-family: var(--font-body);
  text-decoration: underline;
  text-underline-offset: 3px;
}

/* ─── STAT STRIP ─────────────────────────────────── */
.stat-strip {
  background: var(--p700);
  padding: 20px 0;
}
.stat-inner {
  display: flex; justify-content: center;
  max-width: var(--max); margin: 0 auto; padding: 0 20px;
}
.stat-item {
  flex: 1; text-align: center; padding: 0 12px;
  border-right: 1px solid rgba(255,255,255,0.18);
}
.stat-item:last-child { border-right: none; }
.stat-num {
  font-family: var(--font-display);
  font-size: 22px; font-weight: 700;
  color: #fff; display: block; line-height: 1;
}
.stat-lbl {
  font-size: 10px; font-weight: 500;
  color: rgba(255,255,255,0.6);
  margin-top: 5px; line-height: 1.4;
}

/* ─── SECTION WRAPPER ────────────────────────────── */
.section { padding: 52px 20px; max-width: var(--max); margin: 0 auto; }
.section-full { padding: 52px 0; background: var(--bg2); }
.section-full .section { background: none; padding-top: 0; padding-bottom: 0; }

/* ─── FEATURE CARDS ──────────────────────────────── */
.feat-grid {
  display: grid; grid-template-columns: 1fr 1fr; gap: 12px;
  margin-top: 4px;
}
.feat-card {
  background: var(--bg);
  border: 1px solid var(--line);
  border-radius: var(--r-md); padding: 20px 16px;
  transition: box-shadow 0.2s, border-color 0.2s;
}
.feat-card:hover {
  border-color: var(--p300);
  box-shadow: 0 6px 20px rgba(91,33,182,0.09);
}
.feat-icon {
  width: 40px; height: 40px;
  background: var(--p50); border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 18px; margin-bottom: 12px;
}
.feat-title { font-size: 13px; font-weight: 700; color: var(--ink); margin-bottom: 6px; line-height: 1.3; }
.feat-desc { font-size: 11px; color: var(--ink3); line-height: 1.65; }

/* ─── WHO CARDS ──────────────────────────────────── */
.who-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
.who-card {
  border: 1.5px solid var(--line);
  border-radius: var(--r-md); padding: 20px 14px;
  transition: all 0.2s; background: var(--bg);
}
.who-card:hover { border-color: var(--p500); background: var(--p50); }
.who-emoji { font-size: 20px; margin-bottom: 10px; display: block; }
.who-title {
  font-family: var(--font-display);
  font-size: 16px; font-weight: 700;
  color: var(--p700); margin-bottom: 8px; line-height: 1.2;
}
.who-desc { font-size: 11px; color: var(--ink3); line-height: 1.6; margin-bottom: 14px; }
.who-link { font-size: 11px; font-weight: 700; color: var(--p500); }
.who-link:hover { color: var(--p700); }

/* ─── LIGHT SECTION (Why SDI) ────────────────────── */
.dark-section {
  background: var(--bg);
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
  padding: 56px 20px;
}
.dark-section .tag { color: var(--p500); }
.dark-section h2.ttl { color: var(--ink); }
.dark-section h2.ttl em { color: var(--p600); font-style: normal; }
.dark-section .desc { color: var(--ink3); }
.pill-list { display: flex; flex-direction: column; gap: 10px; }
.pill {
  display: flex; align-items: flex-start; gap: 14px;
  background: var(--bg2);
  border: 1px solid var(--line);
  border-radius: var(--r-md); padding: 18px 14px;
  transition: border-color 0.2s, box-shadow 0.2s;
}
.pill:hover {
  border-color: var(--p300);
  box-shadow: 0 4px 16px rgba(91,33,182,0.08);
}
.pill-icon {
  width: 38px; height: 38px; flex-shrink: 0;
  background: var(--p50); border-radius: 10px;
  display: flex; align-items: center; justify-content: center; font-size: 17px;
}
.pill-body h4 { font-size: 13px; font-weight: 700; color: var(--ink); margin-bottom: 5px; line-height: 1.3; }
.pill-body p { font-size: 11px; color: var(--ink3); line-height: 1.65; }

/* ─── RETURN BLOCK ───────────────────────────────── */
.return-block {
  background: var(--p100);
  padding: 48px 20px; text-align: center;
  border-top: 1px solid var(--p300);
  border-bottom: 1px solid var(--p300);
}
.return-lbl { font-size: 12px; color: var(--p600); font-weight: 600; margin-bottom: 8px; }
.return-big {
  font-family: var(--font-display);
  font-size: 80px; font-weight: 900;
  color: var(--p700); line-height: 1;
}
.return-big sup { font-size: 36px; vertical-align: top; margin-top: 16px; display: inline-block; }
.return-note { font-size: 12px; color: var(--ink3); margin-top: 10px; }

/* ─── PORTFOLIO ──────────────────────────────────── */
.port-header {
  display: flex; align-items: flex-end; justify-content: space-between;
  margin-bottom: 20px;
}
.port-value { font-family: var(--font-display); font-size: 26px; font-weight: 700; color: var(--ink); }
.port-change { font-size: 14px; font-weight: 700; color: var(--p600); }
.donut-wrap { display: flex; align-items: center; gap: 18px; margin-bottom: 20px; }
.legend { flex: 1; display: flex; flex-direction: column; gap: 7px; }
.leg-item { display: flex; align-items: center; gap: 8px; }
.leg-dot { width: 9px; height: 9px; border-radius: 50%; flex-shrink: 0; }
.leg-name { font-size: 11px; color: var(--ink2); }
.alloc-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
.alloc-card {
  background: var(--bg); border: 1px solid var(--line);
  border-radius: var(--r-sm); padding: 14px 12px;
}
.alloc-name { font-size: 10px; color: var(--ink3); margin-bottom: 4px; font-weight: 500; }
.alloc-pct { font-size: 16px; font-weight: 700; color: var(--p700); }

/* ─── EXPERTS ────────────────────────────────────── */
.exp-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
.exp-card {
  border: 1px solid var(--line); border-radius: var(--r-md);
  padding: 22px 14px; text-align: center;
  transition: box-shadow 0.2s;
}
.exp-card:hover { box-shadow: 0 6px 20px rgba(91,33,182,0.1); }
.exp-avatar {
  width: 60px; height: 60px; border-radius: 50%;
  background: var(--p50); border: 2px solid var(--p100);
  display: flex; align-items: center; justify-content: center;
  font-size: 24px; margin: 0 auto 12px;
}
.exp-name {
  font-family: var(--font-display);
  font-size: 16px; font-weight: 700;
  color: var(--p700); margin-bottom: 6px; line-height: 1.2;
}
.exp-for { font-size: 11px; color: var(--ink3); margin-bottom: 14px; line-height: 1.5; }
.exp-btn {
  display: inline-block; font-size: 11px; font-weight: 700;
  color: var(--p600); background: var(--p50);
  padding: 7px 14px; border-radius: 8px;
  transition: background 0.18s;
}
.exp-btn:hover { background: var(--p100); }

/* ─── CTA BOTTOM ─────────────────────────────────── */
.cta-bottom {
  background: var(--ink); padding: 60px 20px; text-align: center;
}
.cta-bottom h2 {
  font-family: var(--font-display);
  font-size: clamp(24px, 6vw, 34px); font-weight: 900;
  color: #fff; margin-bottom: 14px; line-height: 1.2;
}
.cta-bottom p { color: rgba(255,255,255,0.55); font-size: 13px; margin-bottom: 28px; line-height: 1.75; max-width: 340px; margin-left: auto; margin-right: auto; margin-bottom: 28px;}
.cta-big {
  display: inline-flex; align-items: center; gap: 8px;
  background: var(--p600); color: #fff; font-weight: 700; font-size: 15px;
  padding: 15px 36px; border-radius: var(--r-md);
  transition: background 0.18s, transform 0.18s;
}
.cta-big:hover { background: var(--p500); transform: translateY(-1px); }

/* ─── FOOTER ─────────────────────────────────────── */
footer {
  background: var(--bg2); border-top: 1px solid var(--line);
  padding: 28px 20px; text-align: center;
  font-size: 11px; color: var(--ink3); line-height: 1.8;
}
footer strong { color: var(--ink2); }

/* ─── ANIMATIONS ─────────────────────────────────── */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(18px); }
  to   { opacity: 1; transform: translateY(0); }
}
.hero-inner > * {
  animation: fadeUp 0.55s cubic-bezier(.22,.68,0,1.2) both;
}
.hero-inner > *:nth-child(1) { animation-delay: 0.05s; }
.hero-inner > *:nth-child(2) { animation-delay: 0.15s; }
.hero-inner > *:nth-child(3) { animation-delay: 0.22s; }
.hero-inner > *:nth-child(4) { animation-delay: 0.28s; }
.hero-inner > *:nth-child(5) { animation-delay: 0.34s; }

/* ─── DIVIDER ────────────────────────────────────── */
.divider { height: 1px; background: var(--line); margin: 0 20px; }

/* ─── RESPONSIVE: max 390px tweaks ──────────────── */
@media (max-width: 360px) {
  .feat-grid, .who-grid, .exp-grid, .alloc-grid { grid-template-columns: 1fr; }
  .stat-num { font-size: 18px; }
  .return-big { font-size: 64px; }
}
@media (min-width: 600px) {
  :root { --max: 540px; }
  .feat-grid { grid-template-columns: repeat(2, 1fr); }
  .hero h1 { font-size: 42px; }
}
</style>
</head>
<body>

<!-- ── NAV ───────────────────────────────────────── -->
<nav>
  <div class="nav-inner">
    <div class="nav-logo">VPS <span>SDI</span></div>
    <a href="#join" class="nav-btn">Tham gia ngay</a>
  </div>
</nav>

<!-- ── HERO ──────────────────────────────────────── -->
<section class="hero">
  <div class="hero-inner">
    <div class="hero-badge">Chiến lược đầu tư AI × Chuyên gia</div>
    <h1>Tài sản của bạn<br>có thể <em>x2</em><br>với chiến lược<br>lợi nhuận hàng đầu</h1>
    <p class="hero-sub">Không cần lựa chọn cổ phiếu. Vượt qua mọi biến động thị trường.</p>
    <p class="hero-sub2">SDI — chiến lược đầu tư cổ phiếu vượt trội với mô hình học máy tối ưu cho từng cá nhân</p>
    <div class="hero-actions">
      <a href="#join" class="btn-primary">Tham gia ngay →</a>
      <button class="btn-outline">Tìm hiểu thêm</button>
    </div>
  </div>
</section>

<!-- ── STAT STRIP ─────────────────────────────────── -->
<div class="stat-strip">
  <div class="stat-inner">
    <div class="stat-item">
      <span class="stat-num">FTSE</span>
      <div class="stat-lbl">Cổ phiếu<br>được lựa chọn</div>
    </div>
    <div class="stat-item">
      <span class="stat-num">+92%</span>
      <div class="stat-lbl">Lợi nhuận tối ưu<br>sau 2 năm</div>
    </div>
    <div class="stat-item">
      <span class="stat-num">AI</span>
      <div class="stat-lbl">Danh mục<br>cá nhân hóa</div>
    </div>
  </div>
</div>

<!-- ── WHY SDI ────────────────────────────────────── -->
<section class="dark-section">
  <div style="max-width:var(--max);margin:0 auto;">
    <div class="tag">Lợi thế vượt trội</div>
    <h2 class="ttl">Chiến lược ưu việt với sự kết hợp giữa <em>Mô hình học máy (AI)</em> và Chuyên gia quản lý Tài sản</h2>
    <p class="desc">Thành công không đến từ sự ngẫu nhiên mà là kết tinh từ bề dày kinh nghiệm của các Chuyên gia quản lý tài sản và ứng dụng các mô hình học máy cùng hàng tỷ dữ liệu đầu tư.</p>
    <div class="pill-list">
      <div class="pill">
        <div class="pill-icon">🏆</div>
        <div class="pill-body">
          <h4>Vượt trội hoàn toàn các Bảng xếp hạng lợi nhuận</h4>
          <p>Dựa trên diễn biến thị trường có nhiều thuận lợi, chúng tôi thay đổi tỷ trọng cổ phiếu tại các chiến lược theo sau.</p>
        </div>
      </div>
      <div class="pill">
        <div class="pill-icon">🔬</div>
        <div class="pill-body">
          <h4>75 kịch bản đầu tư được kiểm chứng</h4>
          <p>Không có kịch bản nào thua lỗ sau 36 tháng đầu tư với SDI — đây là cam kết từ dữ liệu thực tế.</p>
        </div>
      </div>
      <div class="pill">
        <div class="pill-icon">⚡</div>
        <div class="pill-body">
          <h4>Tái cân bằng động theo thị trường</h4>
          <p>Hệ thống tự động điều chỉnh danh mục theo biến động thị trường, không cần bạn theo dõi hàng ngày.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── HOW IT WORKS ───────────────────────────────── -->
<div class="section-full">
  <div class="section">
    <div class="tag">Cách hoạt động</div>
    <h2 class="ttl">SDI ứng dụng mô hình học máy tối ưu cho từng <em>Nhà đầu tư cá nhân</em></h2>
    <p class="desc">Chiến lược SDI tập trung đầu tư vào các cổ phiếu được FTSE lựa chọn và được hệ thống tối ưu cho từng chân dung nhà đầu tư tham gia.</p>
    <div class="feat-grid">
      <div class="feat-card">
        <div class="feat-icon">📊</div>
        <div class="feat-title">Cổ phiếu FTSE lựa chọn</div>
        <div class="feat-desc">Rổ cổ phiếu chất lượng cao được FTSE Russell sàng lọc và xếp hạng toàn cầu.</div>
      </div>
      <div class="feat-card">
        <div class="feat-icon">🤖</div>
        <div class="feat-title">Mô hình học máy</div>
        <div class="feat-desc">AI phân tích hàng triệu dữ liệu để tối ưu danh mục theo profile rủi ro của bạn.</div>
      </div>
      <div class="feat-card">
        <div class="feat-icon">💼</div>
        <div class="feat-title">Danh mục cá nhân hóa</div>
        <div class="feat-desc">Mỗi nhà đầu tư nhận danh mục riêng biệt phù hợp với mục tiêu tài chính.</div>
      </div>
      <div class="feat-card">
        <div class="feat-icon">📈</div>
        <div class="feat-title">Lợi nhuận tối ưu</div>
        <div class="feat-desc">Hiệu suất vượt trội so với các chiến lược đầu tư truyền thống trên thị trường.</div>
      </div>
    </div>
  </div>
</div>

<!-- ── FOR WHO ────────────────────────────────────── -->
<div class="section">
  <div class="tag">Dành cho ai?</div>
  <h2 class="ttl">SDI phù hợp <em>với ai?</em></h2>
  <div class="who-grid">
    <div class="who-card">
      <span class="who-emoji">🌱</span>
      <div class="who-title">Nhà đầu tư mới vào thị trường</div>
      <div class="who-desc">Khách hàng mới gia nhập thị trường, chưa có kinh nghiệm chọn cổ phiếu.</div>
      <a href="#" class="who-link">Xem chi tiết →</a>
    </div>
    <div class="who-card">
      <span class="who-emoji">🎯</span>
      <div class="who-title">Muốn tối ưu lợi nhuận</div>
      <div class="who-desc">Nhà đầu tư có kết quả chưa ổn định, muốn cải thiện hiệu suất danh mục.</div>
      <a href="#" class="who-link">Xem chi tiết →</a>
    </div>
  </div>
</div>

<!-- ── RETURN HIGHLIGHT ───────────────────────────── -->
<div class="return-block">
  <div class="return-lbl">Lợi nhuận lũy kế 2 năm</div>
  <div class="return-big">92<sup>%</sup></div>
  <div class="return-note">Tính đến tháng 4/2026 — Dẫn đầu thị trường</div>
</div>

<!-- ── PORTFOLIO ──────────────────────────────────── -->
<div class="section-full">
  <div class="section">
    <div class="tag">Cơ cấu danh mục</div>
    <h2 class="ttl" style="font-size:20px;">Tài sản được quản lý bởi Chuyên gia</h2>
    <div class="port-header">
      <div>
        <div style="font-size:11px;color:var(--ink3);margin-bottom:4px;font-weight:500;">Tổng giá trị</div>
        <div class="port-value">10,289.72 tỷ</div>
      </div>
      <div class="port-change">+1.7%</div>
    </div>
    <div class="donut-wrap">
      <svg width="108" height="108" viewBox="0 0 108 108" style="flex-shrink:0;">
        <circle cx="54" cy="54" r="38" fill="none" stroke="#E4E4E7" stroke-width="17"/>
        <circle cx="54" cy="54" r="38" fill="none" stroke="#5B21B6" stroke-width="17"
          stroke-dasharray="107.5 131.7" stroke-dashoffset="0" transform="rotate(-90 54 54)"/>
        <circle cx="54" cy="54" r="38" fill="none" stroke="#7C3AED" stroke-width="17"
          stroke-dasharray="83.6 155.6" stroke-dashoffset="-107.5" transform="rotate(-90 54 54)"/>
        <circle cx="54" cy="54" r="38" fill="none" stroke="#A78BFA" stroke-width="17"
          stroke-dasharray="23.9 215.3" stroke-dashoffset="-191.1" transform="rotate(-90 54 54)"/>
        <circle cx="54" cy="54" r="38" fill="none" stroke="#DDD6FE" stroke-width="17"
          stroke-dasharray="23.9 215.3" stroke-dashoffset="-215.0" transform="rotate(-90 54 54)"/>
        <text x="54" y="50" text-anchor="middle" font-size="9.5" fill="#3A3A3A" font-family="Be Vietnam Pro" font-weight="600">Danh</text>
        <text x="54" y="62" text-anchor="middle" font-size="9.5" fill="#3A3A3A" font-family="Be Vietnam Pro" font-weight="600">mục</text>
      </svg>
      <div class="legend">
        <div class="leg-item"><div class="leg-dot" style="background:#5B21B6"></div><div class="leg-name">Cổ phiếu</div></div>
        <div class="leg-item"><div class="leg-dot" style="background:#7C3AED"></div><div class="leg-name">Trái phiếu</div></div>
        <div class="leg-item"><div class="leg-dot" style="background:#A78BFA"></div><div class="leg-name">Tiền mặt</div></div>
        <div class="leg-item"><div class="leg-dot" style="background:#DDD6FE"></div><div class="leg-name">Phái sinh</div></div>
      </div>
    </div>
    <div class="alloc-grid">
      <div class="alloc-card">
        <div class="alloc-name">Trái phiếu CVCK120426</div>
        <div class="alloc-pct">50.56%</div>
      </div>
      <div class="alloc-card">
        <div class="alloc-name">Cổ phiếu phòng thủ</div>
        <div class="alloc-pct">10.56%</div>
      </div>
      <div class="alloc-card">
        <div class="alloc-name">Cổ phiếu ACB</div>
        <div class="alloc-pct">50.56%</div>
      </div>
      <div class="alloc-card">
        <div class="alloc-name">Cổ phiếu VCB</div>
        <div class="alloc-pct">10.56%</div>
      </div>
      <div class="alloc-card">
        <div class="alloc-name">Cổ phiếu TCB</div>
        <div class="alloc-pct">50.56%</div>
      </div>
      <div class="alloc-card">
        <div class="alloc-name">Cổ phiếu HPG</div>
        <div class="alloc-pct">10.56%</div>
      </div>
    </div>
  </div>
</div>

<!-- ── EXPERTS ────────────────────────────────────── -->
<div class="section">
  <div class="tag">Đội ngũ</div>
  <h2 class="ttl">Tài sản được quản lý bởi <em>Chuyên gia</em></h2>
  <div class="exp-grid">
    <div class="exp-card">
      <div class="exp-avatar">🦁</div>
      <div class="exp-name">Warren Buffet</div>
      <div class="exp-for">Khách hàng mới gia nhập thị trường</div>
      <a href="#" class="exp-btn">Xem chi tiết</a>
    </div>
    <div class="exp-card">
      <div class="exp-avatar">⭐</div>
      <div class="exp-name">Kaisa Nguyen</div>
      <div class="exp-for">Nhà đầu tư có kết quả ổn định</div>
      <a href="#" class="exp-btn">Xem chi tiết</a>
    </div>
  </div>
</div>

<!-- ── CTA ────────────────────────────────────────── -->
<section class="cta-bottom" id="join">
  <div style="max-width:var(--max);margin:0 auto;">
    <h2>Bắt đầu hành trình đầu tư thông minh hôm nay</h2>
    <p>Gia nhập hàng nghìn nhà đầu tư đang hưởng lợi từ chiến lược SDI được tối ưu bởi AI và các chuyên gia hàng đầu Việt Nam.</p>
    <a href="#" class="cta-big">Tham gia ngay — Miễn phí</a>
  </div>
</section>

<!-- ── FOOTER ─────────────────────────────────────── -->
<footer>
  <strong>VPS Securities — SDI Strategy</strong><br>
  <span style="margin-top:6px;display:inline-block;">Đầu tư có rủi ro. Lợi nhuận trong quá khứ không đảm bảo kết quả trong tương lai.<br>Vui lòng đọc kỹ tài liệu sản phẩm trước khi quyết định đầu tư.</span>
</footer>

</body>
</html>
